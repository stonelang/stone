#define DEFINE_DIAGNOSTIC_MACROS
#include "DiagnosticMacros.h"


ERROR(error_add_project,none,
      "error creating '%0' because %1", (StringRef, StringRef))

#define UNDEFINE_DIAGNOSTIC_MACROS
#include "DiagnosticMacros.h"
