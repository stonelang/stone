#include <assert.h>

double average(int count, ..., const char *any) { return 0; }
int main() {

  // Decl *d = new Decl();
  // DeclDiagnosticArgument arg(d);

  // delete d;

  // F0(&arg);

  return 0;
}
